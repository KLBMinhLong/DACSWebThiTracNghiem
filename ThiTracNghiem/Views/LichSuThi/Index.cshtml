@model List<LichSuLamBai>

<h3 class="mb-3">Lịch sử thi</h3>

@if (!Model.Any())
{
    <p>Bạn chưa có lịch sử thi nào.</p>
}
else
{
    <table class="table table-bordered">
    <thead>
        <tr>
            <th>Đề thi</th>
            <th>Ngày làm</th>
            <th>Điểm</th>
            <th>Xem lại</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.DeThi.TenDeThi</td>
                <td>@item.NgayBatDau.ToString("dd/MM/yyyy HH:mm")</td>
                <td>@(item.Diem.HasValue ? item.Diem + " điểm" : "Chưa có")</td>
                <td>
                    <a asp-action="XemChiTiet" asp-route-id="@item.Id" class="btn btn-info btn-sm">Xem lại</a>
                </td>
            </tr>
        }
    </tbody>
</table>
}
