@model DeThi
@{
    var danhSachLichSu = ViewBag.LichSuDangLam as List<ThiTracNghiem.Models.LichSuLamBai>;
    var dangLam = danhSachLichSu?.FirstOrDefault(l => l.DeThiId == Model.Id);
}

<div class="container mt-4">
    <h2 class="mb-4 text-center">@Model.TenDeThi</h2>

    <ul class="list-group mb-4">
        <li class="list-group-item"><strong>Số câu hỏi:</strong> @Model.SoLuongCauHoi</li>
        <li class="list-group-item"><strong>Thời gian làm bài:</strong> @Model.ThoiGianLamBai phút</li>
        <li class="list-group-item"><strong>Ngày tạo:</strong> @Model.NgayTao.ToString("dd/MM/yyyy")</li>
    </ul>

    <div class="text-center">
        
        @if (dangLam != null)
        {
            <div class="alert alert-warning">
                🕒 Bạn đang làm dở đề này từ @dangLam.NgayBatDau.ToString("HH:mm dd/MM")
            </div>
            <a href="/Thi/LamBai?id=@Model.Id" class="btn btn-warning">Tiếp tục làm bài</a>
        }
        else
        {
            <a href="/Thi/LamBai?id=@Model.Id" class="btn btn-success">Bắt đầu làm bài</a>
        }
    </div>
</div>
